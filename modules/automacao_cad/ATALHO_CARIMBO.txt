javascript:(()=>{function extractTableValue(label){try{const strongs=Array.from(document.querySelectorAll('td strong'));const targetStrong=strongs.find(el=>el.textContent.trim().toUpperCase()===label.toUpperCase());if(targetStrong){const tdLabel=targetStrong.closest('td');if(tdLabel&&tdLabel.nextElementSibling){return tdLabel.nextElementSibling.textContent.trim().replace(/\s+/g,' ')}}}catch(e){console.error("Error extracting "+label,e)}return""}function isValid(v){return v&&typeof v==="string"&&v.trim()!==""}function limparNumero(v){return v.replace(/^\(\d+\)\s*/,'').trim()}const osNum=extractTableValue("Nº da Solicitação");const nome=extractTableValue("Nome");let celular=extractTableValue("Celular");let telefone=extractTableValue("Telefone");let fone=isValid(celular)?"TEL.: "+celular:(isValid(telefone)?"TEL.: "+telefone:"");let solicitante="";if(isValid(nome)){let nomeLimpo=nome.replace(/^\d{2}\.\d{3}\.\d{3}\s*/,%27%27);solicitante=nomeLimpo+(isValid(fone)?" - "+fone:"")}else{solicitante=fone}const endereco=extractTableValue("Endereço");let numero=extractTableValue("Número");const bairro=extractTableValue("Bairro");const localidade=extractTableValue("Localidade");let enderecoCompleto="";if(isValid(endereco))enderecoCompleto+=endereco;if(isValid(numero)&&!/^\(\d+\)$/.test(numero.trim()))enderecoCompleto+=(enderecoCompleto?" - ":"")+numero;if(isValid(bairro))enderecoCompleto+=(enderecoCompleto?" - ":"")+bairro;if(isValid(localidade))enderecoCompleto+=(enderecoCompleto?" - ":"")+limparNumero(localidade);const tec=extractTableValue("Técnico Projetista").replace(/\.$/,%27%27);const dmEl=document.querySelector("input#dm.form-control");const dmVal=dmEl?dmEl.value.trim():"";const almEl=document.querySelector("input#alimentador.form-control");const almVal=almEl?almEl.value.trim():"";const compEl=document.querySelector("input#instalacao.form-control");const compVal=compEl?compEl.value.trim():"";const servicoEl=document.querySelector("#select2-titulo_projeto-container");const servicoVal=servicoEl?(servicoEl.textContent||servicoEl.value||"").trim():"";const objetivoEl=document.querySelector("#objetivo_projeto");const objetivoVal=objetivoEl?(objetivoEl.value||objetivoEl.textContent||"").trim():"";const nobsEl=document.querySelector("input#numero_odi.form-control");const nobsVal=nobsEl?nobsEl.value.trim().replace(/^(.{3})(.{2})/,"$1-$2-"):"";const desenhoEl=document.querySelector(".col-md-6:nth-child(1) .username");const desenhoRaw=desenhoEl?desenhoEl.textContent.trim():"";const desenhoVal=desenhoRaw.indexOf(":")!==-1?desenhoRaw.substring(desenhoRaw.indexOf(":")+1).trim():desenhoRaw;const orcamentoEl=document.querySelector(".col-md-6:nth-child(2) .username");const orcamentoRaw=orcamentoEl?orcamentoEl.textContent.trim():"";const orcamentoVal=orcamentoRaw.indexOf(":")!==-1?orcamentoRaw.substring(orcamentoRaw.indexOf(":")+1).trim():orcamentoRaw;const regionalEl=document.querySelector("body > div.wrapper > div > section.content > div:nth-child(2) > div > div > div.box-body > div > div > table:nth-child(1) > tbody > tr:nth-child(4) > td:nth-child(6)");const regionalVal=regionalEl?regionalEl.textContent.trim():"";let campos=[];if(isValid(osNum))campos.push("OS: "+osNum);if(isValid(dmVal))campos.push("DM: "+dmVal);if(isValid(almVal))campos.push("ALIM: "+almVal);if(isValid(compVal))campos.push("COMP: "+compVal);if(isValid(servicoVal))campos.push("SERVICO: "+servicoVal);if(isValid(objetivoVal))campos.push("OBJETIVO: "+objetivoVal);if(isValid(nobsVal))campos.push("NOBS: "+nobsVal);if(isValid(desenhoVal))campos.push("DESENHO: "+desenhoVal);if(isValid(orcamentoVal))campos.push("ORCAMENTO: "+orcamentoVal);if(isValid(regionalVal))campos.push("REGIONAL: "+regionalVal);if(isValid(solicitante))campos.push("SOLICITANTE: "+solicitante);if(isValid(enderecoCompleto))campos.push("ENDEREÇO: "+enderecoCompleto);if(isValid(tec))campos.push("TEC: "+tec);let output=campos.join(%27 | %27).toUpperCase();const textarea=document.createElement("textarea");textarea.value=output;document.body.appendChild(textarea);textarea.select();try{document.execCommand(%27copy%27);alert("COPIADO COM SUCESSO!")}catch(e){prompt("Copie manualmente:",output)}document.body.removeChild(textarea)})();